"""Post-processing of camera frames, e.g. lane detection and frame encoding for display.

Does not include functionality that is already built into the StereoLabs Cameras / SDK (e.g. body tracking)."""